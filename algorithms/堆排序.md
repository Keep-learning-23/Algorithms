# 堆排序
[TOC]
## 概念
- 最大堆：除了根节点以外的所有节点，都满足A[Parent(i)] >= A[i]，即父节点的值大于等于子节点的值。
- 最小堆：除了根节点以外的所有节点，都满足A[Parent(i)] <= A[i]，即父节点的值小于等于子节点的值。
### 练习
- 在高度为h的堆中，元素个数最多和最少分别是多少？最多2^h-1，最少2^(h-1)
- 证明：含n个元素的堆的高度为lgn
## 维护堆的性质 - MAX-HEAPIFY
## 建堆 - BUILD-MAX-HEAP
```plantuml
```